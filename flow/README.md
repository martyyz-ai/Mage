# Mage-Flow Project Page

Static project page (Bulma / Nerfies template). Open `index.html` locally or deploy to GitHub Pages.

```bash
python -m http.server 8000   # then visit http://localhost:8000
```
